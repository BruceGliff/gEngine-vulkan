// File for an instantiation all singletons.
// See include/gEng/utils/singletone_base.h for details realization.
#include "gEng/global.h"
#include "../environment/platform_manager.h"

using namespace gEng;

#define INSTANCE(Class) \
template<>\
std::unique_ptr<SingletoneBase<Class>> SingletoneBase<Class>::Mgr{nullptr};

//template<>
//std::unique_ptr<SingletoneBase<GlbManager>> SingletoneBase<GlbManager>::Mgr{nullptr};
INSTANCE(PltManager);
